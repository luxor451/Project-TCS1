{
    // a simple task file to build the application
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cppbuild",
            "label": "build app-linked-list-int",
            "command": "make",
            "args": [
                "app-linked-list-int"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}
