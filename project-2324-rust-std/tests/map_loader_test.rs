/* use std::fs::File;
use std::io;
use std::io::prelude::*;
use std::io::BufReader;
use std::env; */

use projet::map_load::*;
/* use scanf::sscanf;
 */

 
fn test(){
/*     let args: Vec<String> = env::args().collect(); */
/*     let maze = load_map(args[1]);*/
    
}
